name: 'Report LCOV'
description: 'Report code coverage from LCOV files'
author: 'Zennon Gosalvez'
inputs:
  coverage-files:
    description: Coverage files to scan (e.g. `coverage/lcov.*.info`)
    required: true
  artifact-name:
    description: Name of generated coverage report artifact. Default is no artifact created.
    required: false
  minimum-coverage:
    description: Minimum % coverage to pass check. Default `0`.
    required: false
    default: '0'
  github-token:
    description: GitHub token to comment on PR.
    required: false
  working-directory:
    description: Directory containing source files. Default `./`
    required: false
    default: './'
  update-comment:
    description: Whether to update existing comment. Default `false`.
    required: false
    default: 'false'
  list-full-paths:
    description: Whether to list full file paths in details. Default `true`.
    required: false
    default: 'true'
outputs:
  total-coverage:
      description: 'Total coverage from scanned files.'

runs:
  using: 'node16'
  pre: 'dist/pre/index.js'
  pre-if: runner.os == 'linux' || runner.os == 'macOS'
  main: 'dist/main/index.js'
branding:
  icon: umbrella
  color: purple
